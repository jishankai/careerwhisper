.jpkc
  - @courses.each do |course|
    .row[style="margin-top:16px"]
      a.col-md-12.col-sm-12.col-xs-12[href=course.href]
        .box
          = image_tag course.avatar.url(:original), class:"img-responsive"
          .row.info
            span.col-md-12.col-sm-12.col-xs-12
             = course.name
          .row.info
            span.col-md-4.col-sm-4.col-xs-6
              = course.speaker=="" ? "待定" : course.speaker
            i.col-md-8.col-sm-8.col-xs-6.text-right
              = course.start_at
          .row[style="padding:0 2px; margin-bottom:4px"]
            .text-justify
              = course.description
            / p.text-justify
            /   = course.description

  / tbody
  /   - @courses.each do |course|
  /     tr
  /       td = course.name
  /       td = course.description
  /       td = course.speaker
  /       td = course.speaker_intro
  /       td = course.start_at
  /       td = course.end_at
  /       td = link_to 'Show', course
  /       td = link_to 'Edit', edit_course_path(course)
  /       td = link_to 'Destroy', course, data: { confirm: 'Are you sure?' }, method: :delete
